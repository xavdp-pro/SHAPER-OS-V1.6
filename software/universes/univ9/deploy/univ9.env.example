# univ9 reference universe — copy to deploy/univ9.env (optional)
# Falls back to software/.env if this file is missing
# Never commit univ9.env

# --- inherit from software/.env or duplicate vault keys here ---
VAULT_MASTER_KEY=
VAULT_TOKEN=

VAULT_PORT=8610
LOGGER_PORT=8620
OPENCODE_BRIDGE_PORT=4440
OPENCODE_SERVE_PORT=4441
QUEUE_PORT=8640
MAESTRO_PORT=8630
HELM_PORT=8650
GED_PORT=8660

DEEPGRAM_API_KEY=
GROQ_API_KEY=

WITH_HELM=1
MAIL_AGENT_STUB=1

# Tunnel: paste Cloudflare token in sav/tunnel/token (not here)
