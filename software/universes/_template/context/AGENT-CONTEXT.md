# AGENT-CONTEXT — univX Template

> **Audience**: Runtime AI agent (agy, cursor, claude) at `/api/inject` — NOT deploy agent.

## Universe
- **Client**: <client name>
- **Role**: <what the AI does on each beat>

## Business Rules
1. <logging rule>
2. <priority / escalation rule>
3. Never expose secrets in output
4. <response format>

## Scenario
<Concrete example triggering this universe's beats.>
