# Moved

Universe **univ7** now lives at the REMOTE2 root:

```
../../UNIV7/
```

This folder is kept for SHAPER-OS integrated tests (`npm test`).

**Perimeter**: P1+P2 test sandbox — see [`../../docs/PERIMETERS.md`](../../docs/PERIMETERS.md).
